#syntax

def greet(name):
    return f"Hello, {name}!"

name = input("Enter your name: ")
print(greet(name))

def is_even(number):
    return number % 2 == 0

number = len(name)
print(f"{number} is even: {is_even(number)}")

